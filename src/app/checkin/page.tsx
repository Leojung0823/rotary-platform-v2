import { CheckinLinkCapture } from "@/components/events/checkin-link-capture";

export const dynamic = "force-dynamic";

export default function PublicCheckinCapturePage() {
  return <CheckinLinkCapture />;
}
